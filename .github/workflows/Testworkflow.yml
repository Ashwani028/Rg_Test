name: Test_workflow

on:
  push:
    branches:
      - 'main'
jobs:
  job:
    name: Testing
    runs-on: self_hosted
    steps:
      - name: My first step
        run: echo Ashwani
      - name: Checkout
        uses: actions/checkout@v5.0.0
      - name: Install Terraform
        uses: little-core-labs/install-terraform@v2.0.0
        with:
          version: v2.0.0
          
